# 圈叉遊戲

一個純 HTML/CSS/JavaScript 的圈叉遊戲專案，可以直接用瀏覽器開啟。

## 使用方式

開啟 `index.html` 即可開始遊玩。

## 本機 Git 流程練習

這段文字用來示範：在本機建立分支、修改、提交、推送到 GitHub，最後透過 Pull Request 合併。

## 檔案

- `index.html`：頁面結構
- `styles.css`：視覺樣式
- `script.js`：遊戲邏輯
